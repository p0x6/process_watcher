{
  "name": "process_watcher",
  "version": "0.0.1",
  "description": "Node.js addon for process watcher to watch for processes to open",
  "main": "index.js",
  "private": true,
  "gypfile": true,
  "dependencies": {
    "bindings": "~1.5.0",
    "nan": "^2.14.0"
  },
  "scripts": {
    "build": "node-gyp configure build --runtime=electron --arch=ia32 --dist-url=https://electronjs.org/headers",
    "test": "node test.js"
  }
}
